# Copy to .env. Replace 203-0-113-10 with your host's public IPv4 address, hyphens instead of dots.
DOMAIN=drop.203-0-113-10.nip.io

# id:sha256:<hash> entries from: docker compose run --rm relay keygen -id agent1
AGENT_KEYS=
