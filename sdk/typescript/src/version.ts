/** Package version, kept in sync with package.json by the release process. */
export const VERSION = "0.1.0";

/** Default value of the X-Client header. */
export const DEFAULT_CLIENT_NAME = "burndrop-ts/" + VERSION;
